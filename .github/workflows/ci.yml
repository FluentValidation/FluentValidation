name: CI

on:
  push:
    branches:
    - master
  pull_request:
    branches:
      - master

jobs:
  build:
    strategy:
      matrix:
        os: ['windows-2019', 'ubuntu-20.04']
    runs-on: ${{ matrix.os }}
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      with:
        fetch-depth: 0

#    - name: Get SDKs to install
#      shell: pwsh
#      # Collect the SDKs we need by parsing global.json
#      # Supplementary sdks are stored in an "others" array.
#      run: |
#        $json = ConvertFrom-Json (Get-Content global.json -Raw)
#        $sdks = ($json.others + $json.sdk.version) -join ","
#        Write-Output "::set-env name=REQUIRED_SDKS::$sdks"

#    - name: Install .NET SDKs
#      # Official actions/setup-dotnet doesn't support multiple side-by-side SDK installation.
#      # Use this fork until they have an official solution.
#      uses: coderpatros/setup-dotnet@sxs
#      with:
#        dotnet-version: ${{ env.RREQUIRED_SDKS }}
    - name: Install .NET SDKs
      shell: pwsh
      run: ./build.ps1 -t install-dotnet-core
    - name: Build and Test
      run: ./build.ps1
      shell: pwsh
